export enum ConnectionPath {
  API = "http://localhost:3001",
}

export enum ApiEndopnts {
  PRODUCTS = "/products",
  USERS = "/users",
  HISTORY = "/history",
  REGISTER = "/REGISTER",
  SIGNIN = "/SIGNIN",
}
